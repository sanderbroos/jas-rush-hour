class Car:
    """
    Class which handles the information from a specific car
    """
    def __init__(self, name, orientation, col, row, length, colour):
        self.name = name
        self.orientation = orientation
        self.col = col
        self.row = row
        self.length = length
        self.colour = colour